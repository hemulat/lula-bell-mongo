<div id='form'>
  <h3>Please enter the Student's ID</h3>
  <%= simple_form_for @transaction,url: {action: :student} do |form| %>
    <%= form.input :student_id, label: "Student ID", as: :integer, required: true %>
    <%= form.submit "Submit", class: "btn btn-success"%>
  <% end %>
</div>
