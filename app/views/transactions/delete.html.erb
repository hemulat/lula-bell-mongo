<%= link_to("<< Back to Transactions", {:action => 'notice'}, :class => 'back-link') %>

<div class="subjects delete">
  <h2>Delete Transaxtion</h2>

  <!-- <%#= form_for(@subject, :method => 'delete') do |f| %> -->
  <%= form_for(@transaction, :url => transaction_path(@transaction), :method => 'delete') do |f| %>

    <p>Are you sure you want to permanently delete this transaction?</p>

    <div class="form-buttons">
      <%= f.submit("Delete Transaction") %>
    </div>

  <% end %>
</div>
